import React from 'react';
import Input from "../style/Input";    // Input component must be imported first

export default function InputComp() {

        return (
         
                <form>
                <Input type="text" />  {/* Renders Input Component */}
                </form>

        )
}