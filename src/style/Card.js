import styled from 'styled-components' // Required imports

const Card = styled.div`    // This component wraps the whole Calculator
    width: 24.7rem;
    margin: auto;
    margin-top: 10vh;
    padding: auto;
    border-radius: 0px;
    border: 2px solid darkgray;
    background-color: lightgray;
`;

export default Card;  // Card component must be exported so others components may use it