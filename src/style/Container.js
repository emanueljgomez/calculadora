import styled from 'styled-components' // Required imports

const Container = styled.div` // The only goal of this component is to center all elements in the app
  text-align: center;
`;

export default Container;  // Container component must be exported so others components may use it